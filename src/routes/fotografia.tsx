import {createFileRoute} from '@tanstack/react-router'; import {useState} from 'react'; import {X} from 'lucide-react'; import {PageIntro,Placeholder} from '@/components/portfolio-shell';
export const Route=createFileRoute('/fotografia')({head:()=>({meta:[{title:'Fotografía | Isaac Espinoza'},{name:'description',content:'Fotografía de eventos, retratos, producto, contenido comercial y obra creativa.'},{property:'og:title',content:'Fotografía | Isaac Espinoza'},{property:'og:description',content:'Archivo fotográfico de Isaac Espinoza.'},{property:'og:type',content:'website'},{name:'twitter:card',content:'summary_large_image'}]}),component:Page});
const cats=['Eventos','Retratos','Producto','Contenido comercial','Urbano / creativo'];
function Page(){const [active,setActive]=useState<string|null>(null);return <main><PageIntro number="05" title="Fotografía">La imagen ocupa el centro. Esta galería preservará el encuadre y el color original de cada fotografía.</PageIntro><div className="flex gap-6 overflow-x-auto border-b border-foreground px-5 py-5 font-mono text-xs uppercase md:px-10">{cats.map(x=><span className="shrink-0" key={x}>{x}</span>)}</div><section className="grid grid-cols-2 gap-3 p-3 md:grid-cols-12 md:gap-6 md:p-10">{cats.map((x,i)=><button key={x} onClick={()=>setActive(x)} className={`${i%3===0?'md:col-span-7':'md:col-span-5'} text-left`} aria-label={`Abrir ${x}`}><Placeholder label={x} className={i%2?'aspect-[4/5]':'aspect-[3/2]'}/></button>)}</section>{active&&<div role="dialog" aria-modal="true" aria-label={active} className="fixed inset-0 z-[70] grid bg-foreground/95 p-5 text-background"><button onClick={()=>setActive(null)} className="absolute right-5 top-5 grid size-12 place-items-center" aria-label="Cerrar fotografía"><X/></button><Placeholder label={active} className="m-auto h-[80vh] w-full max-w-6xl border-background"/></div>}</main>}